
#include <iostream>
using namespace std;
int main(){
	setlocale(0, "");
	cout << "Продолжение программы “Геометрические фигуры”" << endl;
	cout << "выберите фигуру: " << endl;
	cout << "1- линия 2- квадрат" << endl;
	int m;
	cin >> m;
	if (m == 2) {
		cout << "1полый или 2заполненный" << endl;
	
	}
		int y; cin >> y;
		if (y == 2) {

	int size;


	cout << "задайте размер: ";
			cin >> size;
			
	int b;

	for (int i = 0; i < size; i++ ) {
		for (int j = 0; j < size; j++) {
			cout << "# ";
		}
		cout << endl;
	}
		}
}
